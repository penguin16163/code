#include<bits/stdc++.h>
using namespace std;
long long l,r,k;
int n;
string s[100];
int main()
{
    scanf("s %s %lld %d", &l, &r, &k, &n);
    for (register int i=1; i<=n; +i) scanf("%d",s[i]);
    for (register long long i=l; i<=r; ++i) {
        
    }
}